export interface AboutJsonPayload {
  toolVersion: string;
  command: string;
  productName: string;
  description: string;
  author: string;
  license: string;
  website: string;
  repository: string;
  nuGet: string;
  runtime: string;
}
